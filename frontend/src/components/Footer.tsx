import React from 'react';
import { Box, Typography } from '@mui/material';

const Footer: React.FC = () => (
  <Box sx={{ bgcolor: '#355E92', color: '#fff', p: 2, textAlign: 'center' }}>
    <Typography variant="subtitle2">ALL RIGHTS RESERVED BY KAZ CODE</Typography>
  </Box>
);

export default Footer;